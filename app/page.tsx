export default function HomePage() {
  return (
    <main className="container py-10">
      <h1 className="text-2xl font-semibold">Yang&apos;s Fried Rice</h1>
      <p className="text-muted-foreground mt-2">
        Menu browsing goes here (R15). Environment scaffold only — build the
        menu grid + cart next.
      </p>
    </main>
  );
}
